# Fixed XOR
# Write a function that takes two equal-length buffers and produces their XOR combination.

def fixed_XOR(hexOne, hexTwo):
    xor = int(hexOne, 16) ^ int(hexTwo, 16)
    return '{:x}'.format(xored)
